diff --git a/src/atoms/index.ts b/src/atoms/index.ts
--- a/src/atoms/index.ts
+++ b/src/atoms/index.ts
@@
 }, (get, set, update: Update<SourceID[]>) => {
-  if (get(currentColumnIDAtom) !== "focus") return
   const _ = update instanceof Function ? update(get(currentSourcesAtom)) : update
diff --git a/src/atoms/primitiveMetadataAtom.ts b/src/atoms/primitiveMetadataAtom.ts
--- a/src/atoms/primitiveMetadataAtom.ts
+++ b/src/atoms/primitiveMetadataAtom.ts
@@
             if (id === "focus") return [id, s.filter(k => sources[k]).map(k => sources[k].redirect ?? k)]
-            return [id, initialMetadata[id]]
+            const defaults = initialMetadata[id]
+            const stored = s.filter(k => defaults.includes(k))
+            return [id, [...stored, ...defaults.filter(k => !stored.includes(k))]]
diff --git a/src/components/column/card.tsx b/src/components/column/card.tsx
--- a/src/components/column/card.tsx
+++ b/src/components/column/card.tsx
@@
             <div
               ref={setHandleRef}
-              className={$("btn", "i-ph:dots-six-vertical-duotone", "cursor-grab")}
+              role="button"
+              tabIndex={0}
+              aria-label={`拖动调整${sources[id].name}板块顺序`}
+              title="按住拖动，调整板块顺序"
+              className={$("btn", "i-ph:dots-six-vertical-duotone", "cursor-grab touch-none select-none")}
             />
diff --git a/src/components/column/dnd.tsx b/src/components/column/dnd.tsx
--- a/src/components/column/dnd.tsx
+++ b/src/components/column/dnd.tsx
@@
-import { currentColumnIDAtom, currentSourcesAtom } from "~/atoms"
+import { currentSourcesAtom } from "~/atoms"
@@
   const [items, setItems] = useAtom(currentSourcesAtom)
-  const currentColumnID = useAtomValue(currentColumnIDAtom)
-  const sortable = currentColumnID === "focus"
+  const sortable = true
